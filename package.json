{
  "name": "pa200-functions",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "deploy": "npm run build && func azure functionapp publish pa200-tutorials",
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "echo \"No tests yet...\""
  },
  "dependencies": {
    "@azure/communication-email": "^1.0.0",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/node": "16.x",
    "azure-functions-core-tools": "^4.x",
    "prettier": "^2.8.8",
    "typescript": "^4.0.0"
  }
}
